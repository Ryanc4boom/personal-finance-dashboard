fastapi>=0.115.6,<1.0
uvicorn[standard]>=0.34.0,<1.0
SQLAlchemy>=2.0.36,<2.1
alembic>=1.14.0,<2.0
# psycopg3 (not psycopg2): it ships wheels for current Python versions and is
# the driver SQLAlchemy 2.x targets going forward. URL scheme: postgresql+psycopg://
psycopg[binary]>=3.2.3,<4.0
pydantic>=2.10.4,<3.0
pydantic-settings>=2.7.0,<3.0
plaid-python>=27.0.0,<39.0
redis>=5.2.1,<7.0
# SEC EDGAR + market data transport. httpx over requests for its explicit
# per-phase Timeout object, which matters when one research request fans out to
# several 10-20MB filing downloads that must not share one flat deadline.
httpx>=0.28.1,<0.29
# Imported directly by services/plaid_client to give the Plaid SDK an explicit
# CA bundle. It arrives transitively via httpx either way, but a module that
# imports it needs it declared rather than inherited.
certifi>=2024.7.4
# ES256 verification of the JWT Plaid signs its webhooks with. Needs the
# `crypto` extra's ECDSA support, which cryptography (already required) provides.
PyJWT>=2.10.0
cryptography>=44.0.0
python-dotenv>=1.0.1
